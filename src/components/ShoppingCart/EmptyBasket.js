import { StyleSheet, View } from 'react-native';

export const EmptyBasket = () => {
  return <View style={{ width: '100%', height: '100%', backgroundColor: 'white' }}></View>;
};

const styles = StyleSheet.create({
  main: {},
});
